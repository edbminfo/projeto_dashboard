<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acesso BM Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <style>
        body {
            background: linear-gradient(135deg, #1a73e8 0%, #0d47a1 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            padding: 20px;
        }

        .login-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            overflow: hidden;
            width: 100%;
            max-width: 400px; /* Tamanho bom no PC */
            position: relative;
        }

        .login-header {
            background-color: #fff;
            padding: 40px 30px 20px 30px;
            text-align: center;
        }

        .logo-icon {
            font-size: 50px;
            color: #1a73e8;
            margin-bottom: 10px;
        }

        .login-body {
            padding: 0 30px 40px 30px;
        }

        .form-control-lg {
            border-radius: 10px;
            font-size: 16px;
            padding: 12px 15px;
            background-color: #f8f9fa;
            border: 1px solid #e9ecef;
        }
        
        .form-control-lg:focus {
            background-color: #fff;
            border-color: #1a73e8;
            box-shadow: 0 0 0 4px rgba(26, 115, 232, 0.1);
        }

        .btn-login {
            background: #1a73e8;
            border: none;
            border-radius: 10px;
            padding: 12px;
            font-size: 18px;
            font-weight: 600;
            width: 100%;
            transition: transform 0.2s;
        }

        .btn-login:active { transform: scale(0.98); }

        .footer-text {
            text-align: center;
            margin-top: 20px;
            color: #888;
            font-size: 13px;
        }

        /* Animação suave */
        .fade-in { animation: fadeIn 0.5s ease-in; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body>

    <div class="login-card fade-in">
        <div class="login-header">
            <div class="logo-icon">
                <i class="fas fa-chart-pie"></i>
            </div>
            <h4 class="fw-bold text-dark">Bem-vindo</h4>
            <p class="text-muted">Acesse seu painel gerencial</p>
        </div>

        <div class="login-body">
            <% if (erro) { %>
                <div class="alert alert-danger d-flex align-items-center" role="alert">
                    <i class="fas fa-exclamation-circle me-2"></i>
                    <div><%= erro %></div>
                </div>
            <% } %>

            <form action="/auth/solicitar" method="POST">
                <div class="mb-4">
                    <label class="form-label fw-bold text-secondary ms-1">WhatsApp / Telefone</label>
                    <div class="input-group">
                        <span class="input-group-text bg-light border-0" style="border-radius: 10px 0 0 10px;">
                            <i class="fas fa-phone-alt text-muted"></i>
                        </span>
                        <input type="tel" name="telefone" 
                               class="form-control form-control-lg border-start-0" 
                               placeholder="(DDD) 99999-9999" 
                               required 
                               style="border-radius: 0 10px 10px 0;">
                    </div>
                    <div class="form-text ms-1">Enviaremos um código de acesso.</div>
                </div>

                <button type="submit" class="btn btn-primary btn-login text-white shadow-sm">
                    Receber Código <i class="fas fa-arrow-right ms-2"></i>
                </button>
            </form>

            <div class="footer-text">
                BM Dashboard &copy; <%= new Date().getFullYear() %>
            </div>
        </div>
    </div>

</body>
</html>