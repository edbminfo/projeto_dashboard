<!doctype html>
<html lang="pt-br">

<head>
    <meta charset="utf-8" />
    <meta name="viewport"
        content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0" />

    <title>Verificar Código - BM Dashboard</title>

    <meta name="description" content="" />
    <link rel="icon" type="image/x-icon" href="/assets/img/favicon/favicon.ico" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Public+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap"
        rel="stylesheet" />

    <link rel="stylesheet" href="/assets/vendor/fonts/iconify-icons.css" />
    <link rel="stylesheet" href="/assets/vendor/css/core.css" />
    <link rel="stylesheet" href="/assets/vendor/css/pages/page-auth.css" />

    <script src="/assets/vendor/js/helpers.js"></script>
    <script src="/assets/js/config.js"></script>
</head>

<body>
    <div class="container-xxl">
        <div class="authentication-wrapper authentication-basic container-p-y">
            <div class="authentication-inner">
                
                <% if (erro) { %>
                    <div class="alert alert-danger d-flex align-items-center mb-3" role="alert">
                        <i class="bx bx-error-circle me-2"></i>
                        <div><%= erro %></div>
                    </div>
                <% } %>

                <div class="card px-sm-6 px-0">
                    <div class="card-body">
                        
                        <div class="app-brand justify-content-center">
                            <span class="app-brand-link gap-2">
                                <span class="app-brand-logo demo">
                                    <img src="/assets/img/icons/logo.svg" alt="Logo" width="40" height="40" />
                                </span>
                                <span class="app-brand-text demo text-heading fw-bold">Bm Dashboard</span>
                            </span>
                        </div>
                        <h4 class="mb-1 text-center">Verificação em Duas Etapas</h4>
                        <p class="text-center mb-4">
                            Enviamos um código para o número <br>
                            <strong class="text-heading"><%= telefone %></strong>
                        </p>

                        <% if (locals.horaServidor) { %>
                            <div class="alert alert-warning d-flex align-items-center p-2 mb-4" role="alert">
                                <i class='bx bx-time-five me-2'></i>
                                <div class="small">
                                    Hora Servidor: <strong><%= horaServidor %></strong>
                                </div>
                            </div>
                        <% } %>

                        <form id="formValidar" action="/auth/validar" method="POST" class="mb-6">
                            
                            <div class="mb-6">
                                <label for="codigo" class="form-label text-center w-100">Digite o código de 6 dígitos</label>
                                <input type="text" 
                                       class="form-control form-control-lg text-center fw-bold fs-3" 
                                       id="codigo" 
                                       name="codigo" 
                                       placeholder="000000" 
                                       maxlength="6" 
                                       inputmode="numeric" 
                                       pattern="[0-9]*"
                                       style="letter-spacing: 0.5rem;" 
                                       autofocus required />
                            </div>

                            <button class="btn btn-primary d-grid w-100" type="submit" id="btnSubmit">
                                <span class="d-flex align-items-center justify-content-center">
                                    <span id="btnText">Validar Acesso</span>
                                    <div id="btnSpinner" class="spinner-border spinner-border-sm text-white ms-2 d-none" role="status">
                                        <span class="visually-hidden">Loading...</span>
                                    </div>
                                </span>
                            </button>
                        </form>

                        <div class="text-center">
                            <p class="mb-0">Não recebeu o código?</p>
                            <a href="/login" class="d-flex align-items-center justify-content-center text-primary">
                                <i class="bx bx-chevron-left scaleX-n1-rtl me-1"></i>
                                Voltar / Tentar novamente
                            </a>
                        </div>
                    </div>
                </div>
                </div>
        </div>
    </div>

    <script src="/assets/vendor/js/bootstrap.js"></script>
    <script src="/assets/vendor/libs/jquery/jquery.js"></script>

    <script>
        $(document).ready(function () {
            $('#codigo').on('input', function() {
                this.value = this.value.replace(/[^0-9]/g, '');
            });

            $('#formValidar').on('submit', function () {
                $('#btnSpinner').removeClass('d-none');
                $('#btnSubmit').prop('disabled', true);
                $('#btnText').text('Validando...');
            });
        });
    </script>
</body>
</html>